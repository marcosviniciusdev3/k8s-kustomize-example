---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: test-web
spec:
  gatewayClassName: example
  listeners:
    - protocol: HTTP
      port: 80
      name: test-web-gw
      allowedRoutes:
        namespaces:
          from: Same
